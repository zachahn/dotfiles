source ~/.zprezto/runcoms/zshrc

if is-callable 'dircolors'; then
  alias ls='ls --group-directories-first'
  alias ls='ls --color=auto'
else
  alias ls='ls -G'
  export CLICOLOR=1
  export LSCOLORS=ExFxBxDxCxegedabagacad

  # print ACL
  alias ls='ls -e'
fi

unsetopt INC_APPEND_HISTORY
unsetopt SHARE_HISTORY

alias lah='ls -lah'
alias l='ls'
alias sl='ls'
alias s='sudo'
alias dush='du -sh'
alias be='bundle exec'
alias hr="hr '💄 ' '🎀 ' '👠 ' '👛 '"
alias HR="hr '💄 ' '🎀 ' '👠 ' '👛 ' '💄 ' '🎀 ' '👠 ' '👛 ' '💄 ' '🎀 ' '👠 ' '👛 ' '💄 ' '🎀 ' '👠 ' '👛 ' '💄 ' '🎀 ' '👠 ' '👛 '"
alias soxxxdreams='ssh -D 9999 za' # socks proxy
alias reload!='source ~/.zshrc'
alias update!='zsh -c "cd ~/.dotfiles; rake"'
alias xxh='ssh -o PubkeyAuthentication=no'
alias ql='qlmanage -p "$@" >& /dev/null'
alias gits='git status'
alias gs='git status'
alias gc='git commit -v'
alias gac='git commit -av'

# NERD

alias badrockets="rubocop --only Style/HashSyntax -a"

# BOXEN

[ -f /opt/boxen/env.sh ] && source /opt/boxen/env.sh

# PATH

export PATH="$HOME/.bin/:$PATH"

# Calculator
calc() { awk "BEGIN{ print $* }"; }

# PDF man pages
pman() { man -t "${1}" | open -f -a /Applications/Preview.app/ }

# Show/hide desktop
desktop() {
  if [[ $1 == "show" ]]; then
    defaults write com.apple.finder CreateDesktop -bool true
    killall Finder
  elif [[ $1 == "hide" ]]; then
    defaults write com.apple.finder CreateDesktop -bool false
    killall Finder
  else
    echo "usage: $0 [show|hide]"
    echo "  show/hide desktop icons"
  fi
}

[ -f ~/.zshrc_local ] && source ~/.zshrc_local

export PATH=".git/safe/../../bin:$PATH"
